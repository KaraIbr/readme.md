IAM_DATABASE_URL=sqlite+aiosqlite:///./ventura.db
IAM_ENVIRONMENT=development
# Generate a strong key: python -c "import secrets; print(secrets.token_urlsafe(32))"
IAM_JWT_SECRET_KEY=replace-with-a-strong-random-key
